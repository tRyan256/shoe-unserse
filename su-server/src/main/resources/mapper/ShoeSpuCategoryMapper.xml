<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.su.mapper.ShoeSpuCategoryMapper">
    <insert id="insert">
        insert into shoe_spu_category (spu_id, category_id, create_time)
        values (#{spuId}, #{categoryId}, #{createTime})
    </insert>

    <insert id="insertBatch">
        insert into shoe_spu_category (spu_id, category_id, create_time)
        values
        <foreach item="item" collection="list" separator=",">
            (#{item.spuId}, #{item.categoryId}, #{item.createTime})
        </foreach>
    </insert>

    <delete id="deleteBySpuId">
        delete from shoe_spu_category where spu_id = #{spuId}
    </delete>

    <delete id="deleteBySpuIds">
        delete from shoe_spu_category where spu_id in
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="getSpuIdsByCategoryIds" resultType="java.lang.Long">
        select distinct spu_id from shoe_spu_category
        where category_id in
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
