<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.su.mapper.ShoeSkuMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into shoe_sku (spu_id, color_name, price, image, is_default, status, create_time, update_time)
        values (#{spuId}, #{colorName}, #{price}, #{image}, #{isDefault}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch">
        insert into shoe_sku (spu_id, color_name, price, image, is_default, status, create_time, update_time)
        values
        <foreach item="item" collection="list" separator=",">
            (#{item.spuId}, #{item.colorName}, #{item.price}, #{item.image}, #{item.isDefault}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <update id="update">
        update shoe_sku
        <set>
            <if test="colorName != null">
                color_name = #{colorName},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="isDefault != null">
                is_default = #{isDefault},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
        delete from shoe_sku where id = #{id}
    </delete>

    <delete id="deleteBySpuId">
        delete from shoe_sku where spu_id = #{spuId}
    </delete>

    <delete id="deleteBySpuIds">
        delete from shoe_sku where spu_id in
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="getByBundleId" resultType="com.su.entity.ShoeSku">
        select distinct s.* from shoe_sku s
        inner join bundle_shoe bs on s.id = bs.sku_id
        where bs.bundle_id = #{bundleId}
    </select>

    <select id="listBySpuIdWithSpuStatus" resultType="com.su.vo.ShoeSkuVO">
        select 
            sk.id,
            sk.spu_id as spuId,
            sp.name as spuName,
            sk.color_name as colorName,
            sk.price,
            sk.image,
            sk.status,
            sp.status as spuStatus,
            sk.is_default as isDefault
        from shoe_sku sk
        left join shoe_spu sp on sk.spu_id = sp.id
        where sk.spu_id = #{spuId}
        order by sk.is_default desc, sk.id asc
    </select>
</mapper>
