<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.su.mapper.BundleMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into bundle (name, price, status,
                            image, description, create_time, update_time, create_user, update_user)
        values (#{name}, #{price}, #{status},
                #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <select id="list" parameterType="Bundle" resultType="Bundle">
        select * from bundle
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>
    <update id="update">
        update bundle
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="delete">
        delete from bundle where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="pageQuery" resultType="com.su.vo.BundleVO">
        select b.*,
               (select count(*) from bundle_shoe where bundle_id = b.id) as shoe_count
        from bundle b
        <where>
            <if test="name != null and name != ''">
                and b.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and b.status = #{status}
            </if>
        </where>
        order by b.update_time desc
    </select>
    <select id="getSellingCount" resultType="java.lang.Integer">
        select count(*)
        from bundle
        where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        and status = 1
    </select>
    <select id="getByIds" resultType="com.su.entity.Bundle">
        select * from bundle where id in
        <foreach item="id" collection="ids" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from bundle
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
</mapper>

