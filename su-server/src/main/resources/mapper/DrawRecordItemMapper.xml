<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.su.mapper.DrawRecordItemMapper">
    <insert id="insertBatch">
        insert into draw_record_item (record_id, sku_id, shoe_size, copies, create_time)
        values
        <foreach collection="items" item="item" separator=",">
            (#{item.recordId}, #{item.skuId}, #{item.shoeSize}, #{item.copies}, #{item.createTime})
        </foreach>
    </insert>

    <delete id="deleteByRecordId">
        delete from draw_record_item where record_id = #{recordId}
    </delete>

    <select id="listByRecordId" resultType="com.su.entity.DrawRecordItem">
        select *
        from draw_record_item
        where record_id = #{recordId}
        order by id asc
    </select>
</mapper>

