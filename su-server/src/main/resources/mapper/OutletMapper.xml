<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.su.mapper.OutletMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into outlet
        (name, code, type, longitude, latitude, address, phone, business_hours, province, city, district, status, create_time, update_time)
        values
        (#{name}, #{code}, #{type}, #{longitude}, #{latitude}, #{address}, #{phone}, #{businessHours}, #{province}, #{city}, #{district}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        update outlet
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="code != null">
                code = #{code},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="longitude != null">
                longitude = #{longitude},
            </if>
            <if test="latitude != null">
                latitude = #{latitude},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="businessHours != null">
                business_hours = #{businessHours},
            </if>
            <if test="province != null">
                province = #{province},
            </if>
            <if test="city != null">
                city = #{city},
            </if>
            <if test="district != null">
                district = #{district},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from outlet where id = #{id}
    </delete>

    <select id="pageQuery" resultType="com.su.entity.Outlet">
        select * from outlet
        <where>
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="code != null and code != ''">
                and code = #{code}
            </if>
            <if test="type != null">
                and type = #{type}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="province != null and province != ''">
                and province = #{province}
            </if>
            <if test="city != null and city != ''">
                and city = #{city}
            </if>
        </where>
        order by update_time desc, id desc
    </select>
</mapper>

